#ifndef __GAME_H__
#define __GAME_H__

//declaramos una enumeración que almacenará los botones o teclas que usaremos en el juego... por el momento solamente usaremos las 4 teclas de dirección.
enum MYKEYS {
       KEY_THROTTLE, KEY_BRAKE, KEY_ROTATE_LEFT, KEY_ROTATE_RIGHT
};

#endif

